# Google AI Studio API key. Server-side only — never prefix with NEXT_PUBLIC_.
# https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Model ids. Kept in env because the Gemini model line moves quickly and
# swapping one should be a config change, not a code change.
#
# Analysis, checklist and comparison need document understanding; Q&A is a
# grounded lookup over text already extracted, so it leads with the cheaper lite
# model. Each is a comma-separated chain tried in order: the newest Flash models
# answer 503 on the free tier when demand spikes, and a request falls through to
# the next model rather than failing. These match the defaults in lib/env.ts.
GEMINI_MODEL_ANALYZE=gemini-3.5-flash,gemini-3.5-flash-lite
GEMINI_MODEL_QA=gemini-3.5-flash-lite,gemini-3.5-flash

# Neon Postgres connection string (pooled). Optional in development: without it
# the app still analyses documents, it just cannot save or share them.
# https://console.neon.tech
DATABASE_URL=

# Optional. Signs the clause context that follow-up questions send back, so
# /api/ask and /api/checklist answer only over context this server produced.
# Leave it empty and a key is derived from GEMINI_API_KEY instead, which means
# the protection is on whether or not this is set. Any long random string.
ASK_CONTEXT_SECRET=
